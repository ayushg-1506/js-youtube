const name = "ayush"
const age = 18

console.log(`hello my name is ${name} and my age is ${age}`);

// const name = new String("ayush gupta") , this creates a string object which is diff from above , rarely used 

const game  = "valorant"

console.log(game[0]);
console.log(game.__proto__);
console.log(game.length);
console.log(game.toUpperCase());
console.log(game.includes("lo"));
console.log(game.indexOf("r"));
console.log(game.slice(0,4));
console.log(game.split(";")); // works if seprator is present in the word
console.log(game.trim()); // removes spaces from front and back
console.log(game.replace("va","wo")); 
console.log(game.repeat(3)); 







